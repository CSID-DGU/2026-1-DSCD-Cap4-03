# AWS 배포 정리

## 배포 목표

FastAPI 백엔드, MySQL DB, Neo4j 추천 그래프, S3 이미지 업로드를 AWS에서 동작시키는 구성임.

권장 구조:

```text
Frontend
  -> FastAPI EC2
        -> S3 이미지 업로드 presigned URL 발급
        -> MySQL 또는 RDS 데이터 조회/저장
        -> Neo4j Docker 추천 그래프 조회
```

## 권장 AWS 구성

### 1. EC2

역할:

- FastAPI 서버 실행
- Neo4j Docker 컨테이너 실행
- 추천 파이프라인 실행

권장:

- Ubuntu 22.04
- 최소 `t3.large`
- 디스크 최소 30GB

보안 그룹 인바운드:

| 포트 | 용도 | 허용 대상 |
|---|---|---|
| 22 | SSH 접속 | 개발자 IP |
| 80 | HTTP | 전체 또는 프론트 도메인 |
| 443 | HTTPS | 전체 또는 프론트 도메인 |
| 8000 | FastAPI 테스트용 | 개발 중에만 제한 허용 |
| 7474 | Neo4j Browser | 개발 중에만 개발자 IP |
| 7687 | Neo4j Bolt | 외부 공개 금지 |

주의:

- `7687`은 FastAPI가 같은 EC2 내부에서 `localhost`로 접근하면 되므로 외부 공개 불필요함.
- 운영에서는 `8000`도 외부 공개보다 Nginx를 통해 `80/443`으로 연결하는 방식 권장.

## 2. MySQL / RDS

### 선택지 A. 개발/시연용: EC2 내부 MySQL

장점:

- 설정 쉬움
- 비용 낮음

단점:

- 서버 장애 시 DB도 같이 영향 받음
- 백업/복구 직접 관리 필요

### 선택지 B. 권장: AWS RDS MySQL

장점:

- DB 백업/복구 관리 쉬움
- 서버와 DB 분리 가능
- 운영에 더 안정적

단점:

- 비용 발생
- 보안 그룹 설정 필요

현재 추천:

- 프로젝트 시연 전까지는 EC2 내부 MySQL 가능
- 최종 배포 안정성을 생각하면 RDS 전환 권장

## 3. S3

역할:

- 사용자가 업로드한 피부 이미지 저장
- 프론트가 presigned URL로 S3에 직접 업로드
- 백엔드는 `s3_key`, `storage_url`만 DB에 저장

현재 버킷 예시:

```text
rouple-images-dev-119236417633-ap-northeast-2-an
```

권장 설정:

- 리전: `ap-northeast-2`
- 퍼블릭 액세스 차단 유지 권장
- 프론트 직접 업로드는 presigned URL 사용
- 이미지 조회 방식은 추후 선택 필요

이미지 조회 선택지:

| 방식 | 설명 |
|---|---|
| public URL | 구현 쉬움, 공개 이미지가 됨 |
| presigned GET URL | 보안 좋음, 백엔드에서 조회 URL 발급 필요 |

현재 개발 단계에서는 public URL 방식으로 진행 가능함.

## 4. Neo4j Docker

추천 모델의 KG 파이프라인이 Neo4j를 사용함.

현재 코드 기준:

```text
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=...
```

EC2에 Docker 설치:

```bash
sudo apt update
sudo apt install -y docker.io
sudo systemctl enable docker
sudo systemctl start docker
sudo usermod -aG docker ubuntu
```

Neo4j 실행:

```bash
docker run -d \
  --name rouple-neo4j \
  -p 7474:7474 \
  -p 7687:7687 \
  -e NEO4J_AUTH=neo4j/비밀번호 \
  -v rouple_neo4j_data:/data \
  -v rouple_neo4j_logs:/logs \
  neo4j:5
```

확인:

```bash
docker ps
```

Neo4j Browser:

```text
http://EC2_PUBLIC_IP:7474
```

주의:

- `7474`는 개발 중에만 개발자 IP로 열기
- `7687`은 외부 공개하지 않는 방향 권장
- 비밀번호는 `.env`에만 저장

## 5. 백엔드 배포 순서

EC2 접속:

```bash
ssh -i 키파일.pem ubuntu@EC2_PUBLIC_IP
```

프로젝트 받기:

```bash
git clone 레포주소
cd 프로젝트/backend
```

가상환경 생성:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

`.env` 생성:

```bash
cp .env.example .env
nano .env
```

필수 설정:

```env
USE_LOCAL_SQLITE=false

MYSQL_HOST=RDS_ENDPOINT_또는_127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=DB_USER
MYSQL_PASSWORD=DB_PASSWORD
MYSQL_DB=Rouple_db

S3_BUCKET=버킷명
S3_REGION=ap-northeast-2
S3_PUBLIC_BASE_URL=https://버킷명.s3.ap-northeast-2.amazonaws.com
AWS_ACCESS_KEY_ID=발급키
AWS_SECRET_ACCESS_KEY=비밀키

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=Neo4j비밀번호

FRONTEND_ORIGINS=http://프론트주소,https://프론트도메인
```

주의:

- `.env`는 절대 GitHub에 커밋 금지
- AWS 키가 노출되면 즉시 비활성화/재발급 필요

## 6. DB 준비

### EC2 내부 MySQL 사용 시

MySQL 설치:

```bash
sudo apt install -y mysql-server
sudo systemctl enable mysql
sudo systemctl start mysql
```

DB 생성:

```sql
CREATE DATABASE Rouple_db CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
```

dump 복원:

```bash
mysql -u root -p Rouple_db < rouple_db_dump.sql
```

### RDS 사용 시

RDS 생성 후:

- 엔진: MySQL
- 리전: 서울
- 퍼블릭 액세스: 가능하면 비활성
- 보안 그룹: EC2 보안 그룹에서만 3306 허용

EC2에서 복원:

```bash
mysql -h RDS_ENDPOINT -u DB_USER -p Rouple_db < rouple_db_dump.sql
```

## 7. 추천 모델 준비

추천 API가 실제 모델로 동작하려면 아래 조건 필요:

- MySQL에 `product`, `ingredient`, `product_ingredient`, `product_review` 적재 완료
- MySQL에 `recommendation_candidate` 데이터 존재
- Neo4j에 정적 그래프 데이터 적재 완료
- FastAPI에서 Neo4j 접속 가능

Neo4j 정적 그래프 적재 예시:

```bash
cd 프로젝트
source backend/.venv/bin/activate
python -u -B -m model.recommendation.kg_pipeline.neo4j_skincare.graph.load_graph --mode static
```

후보 데이터 확인:

```sql
SELECT COUNT(*) FROM recommendation_candidate;
```

현재 로컬 확인 기준:

```text
recommendation_candidate = 0
```

따라서 embedding/retrieval 후보 생성 단계 연결 필요함.

## 8. FastAPI 실행

개발 확인용:

```bash
cd backend
source .venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

접속:

```text
http://EC2_PUBLIC_IP:8000/docs
```

운영 권장:

- `systemd`로 FastAPI 서비스 등록
- Nginx로 `80/443` 연결
- HTTPS 인증서 적용

## 9. systemd 서비스 예시

서비스 파일 생성:

```bash
sudo nano /etc/systemd/system/rouple-backend.service
```

내용:

```ini
[Unit]
Description=Rouple FastAPI Backend
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/프로젝트/backend
Environment="PATH=/home/ubuntu/프로젝트/backend/.venv/bin"
ExecStart=/home/ubuntu/프로젝트/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

실행:

```bash
sudo systemctl daemon-reload
sudo systemctl enable rouple-backend
sudo systemctl start rouple-backend
sudo systemctl status rouple-backend
```

로그 확인:

```bash
journalctl -u rouple-backend -f
```

## 10. Nginx 연결 예시

설치:

```bash
sudo apt install -y nginx
```

설정:

```bash
sudo nano /etc/nginx/sites-available/rouple
```

내용:

```nginx
server {
    listen 80;
    server_name 서버도메인_또는_EC2_PUBLIC_IP;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

활성화:

```bash
sudo ln -s /etc/nginx/sites-available/rouple /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

## 11. 배포 후 확인 순서

1. FastAPI health 확인

```text
GET /health
```

2. Swagger 접속

```text
http://서버주소/docs
```

3. 회원가입/로그인 확인

```text
POST /auth/signup
POST /auth/login
GET /users/me
```

4. S3 presigned URL 확인

```text
POST /files/presign
```

5. 상품 조회 확인

```text
GET /products
GET /products/{product_id}
```

6. 추천 실행 전 확인

```sql
SELECT COUNT(*) FROM recommendation_candidate;
```

7. 추천 실행 확인

```text
POST /recommendations
GET /recommendations/{session_id}
```

## 12. 비용 주의

비용 발생 가능 항목:

- EC2 실행 시간
- EBS 디스크
- RDS 실행 시간
- S3 저장 용량/요청 수
- 데이터 전송량

절약 팁:

- 개발 안 할 때 EC2/RDS 중지
- Budgets에서 월 예산 알림 설정
- S3에 큰 이미지 반복 업로드 주의
- 불필요한 스냅샷 삭제

## 13. 현재 남은 배포 전 작업

- Neo4j 설정을 코드 하드코딩에서 `.env` 기반으로 변경 필요
- `recommendation_candidate` 생성 로직 연결 필요
- 피부분석 결과를 DB에 직접 저장하도록 전환 필요
- 추천 LLM 설명 실제 연결 필요
- 운영용 CORS 도메인 확정 필요
- RDS 사용 여부 결정 필요
- HTTPS 적용 여부 결정 필요
